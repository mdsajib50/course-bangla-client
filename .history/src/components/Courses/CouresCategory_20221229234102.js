import React from 'react';

const CouresCategory = ({courses}) => {
    console.log(courses)
    return (
        <div>
            courses.map(course)
        </div>
    );
};

export default CouresCategory;