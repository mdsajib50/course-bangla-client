import React from 'react';

const Checkout = () => {
    const course = useLoaderData
    return (
        <div>
            <h1>This is checkout</h1>
        </div>
    );
};

export default Checkout;